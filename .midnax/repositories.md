# Repositories

- **FernandoPazCavalcante/lazyswap** (default branch: `master`) — **lazyswap** is a self-custody terminal wallet — a Vim-style **TUI and a non-interactive CLI** — that executes crypto swaps directly on-chain from your machine. No exchange account, no custodian. I…
- **FernandoPazCavalcante/lazyswap-contracts** (default branch: `master`) — `lazyswap-contracts` is the Solidity smart-contract repository for the LazySwap project. It contains a single production contract, **`FeeVault`**, which receives swap referral fees from DEX aggrega…
- **FernandoPazCavalcante/lazyswap-site** (default branch: `master`) — The **lazyswap-site** repo is the marketing landing page and install-script host for [LazySwap](https://github.com/FernandoPazCavalcante/lazyswap-tui) — a vim-style terminal DeFi swap TUI ("the laz…
